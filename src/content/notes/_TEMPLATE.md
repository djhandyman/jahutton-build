---
# Copy this file, rename it to the URL you want, and fill this in.
# The filename IS the address: my-slug.md → /notes/my-slug/
# Kebab-case, no date in the filename, no subfolders.
title: The title, as it appears at the top of the page and in the feed
description: One sentence. It's the card on /notes, the Google result, the social card, and the RSS summary. Write it last.
date: 2026-08-03
# draft: true    ← uncomment while you're still working. A draft shows up on
#                  http://localhost:4321/notes/ with a Draft badge and never
#                  appears on the live site — no page, no sitemap entry, no feed item.
# pinned: true   ← uncomment for an evergreen piece that should sit in "Start here"
#                  above the stream. Buyer questions, not build logs. Keep it to 3–4.
---

Write the note here. Plain markdown.

## A subheading is `##`, not `#`

The `#` level is the title above — it comes from the frontmatter, not the body.

Images go in `public/images/notes/` and are referenced as
`![alt text](/images/notes/whatever.jpg)`. Check for GPS data before publishing any
photo of a private place.
